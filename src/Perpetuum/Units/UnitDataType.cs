namespace Perpetuum.Units
{
    public enum UnitDataType
    {
        Decay, // byte
        ConstructionLevelCurrent, // int
        Tint, // r + g + b (byte)
        MissionGuid, // 16 bytes
        MissionDisplayOrder // int
    }
}